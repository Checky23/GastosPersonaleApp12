<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.GastosPersonales"
        parent="android:Theme.Material.Light.NoActionBar">

        <item name="android:windowBackground">@android:color/white</item>

    </style>

    <!-- SPLASH -->
    <style name="Theme.GastosPersonales.Starting"
        parent="Theme.SplashScreen">

        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo_splash_reducido</item>
        <item name="postSplashScreenTheme">@style/Theme.GastosPersonales</item>

    </style>

</resources>